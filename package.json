{
  "name": "viral-score-server",
  "version": "1.0.0",
  "description": "Viral Score Server - Social media engagement scoring for DeFi pools",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "drizzle-orm": "^0.38.0",
    "postgres": "^3.4.5",
    "viem": "^2.21.0",
    "@openzeppelin/merkle-tree": "^1.0.7",
    "cron": "^3.1.7"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.6.0"
  }
}

